<%= form_for(@indicator_federation) do |f| %>
  <% if @indicator_federation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@indicator_federation.errors.count, "error") %> prohibited this indicator_federation from being saved:</h2>

      <ul>
      <% @indicator_federation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
