<%= form_for(@perfomance) do |f| %>
  <% if @perfomance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@perfomance.errors.count, "error") %> prohibited this perfomance from being saved:</h2>

      <ul>
      <% @perfomance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :junior_companies_id %><br>
    <%= collection_select(:perfomance, :junior_companies_id, @junior_companies, :id, :name, {:prompt => false })  %>
  </div>
  <div class="field">
    <%= f.label :federations_id %><br>
    <%= collection_select(:perfomance, :federations_id, @federations, :id, :name, {:prompt => false })  %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br>
    <%= collection_select(:perfomance, :role_id, @roles, :id, :name, {:prompt => false })  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
